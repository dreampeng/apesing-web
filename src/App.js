import React, {Component, Fragment} from 'react';
import {observer} from 'mobx-react';

@observer
class App extends Component {
    render() {
        return (
            <Fragment className="ApeSing">
                ApeSing
            </Fragment>
        );
    }
}

export default App;
